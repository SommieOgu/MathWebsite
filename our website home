<!DOCTYPE html>
<html>
<head>
    <style>
        .button {
            border: none;
            color: white;
            padding: 16px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            transition-duration: 0.4s;
            cursor: pointer;
        }

        .button1 {
        background-color: white;
        color: #04AA6D;
        border: 2px solid #04AA6D;33
        font-weight: bold;
        padding: 14px 28px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
        transition: all 0.3s ease;
                }

    .button1:hover {
        background-color: #04AA6D;
        color: white;
        box-shadow: 0 6px 16px rgba(4, 170, 109, 0.4);
        transform: scale(1.05);
    }


        .button2 {
            background-color: white; 
            color: black; 
            border: 2px solid #008CBA;
        }

        .button2:hover {
            background-color: #008CBA;
            color: white;
        }

        /* Position the button at the bottom of the page */
        .button-container {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
        }


        /* Modal (hidden by default) */
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            backdrop-filter: blur(5px); /* Blurred background */
            background-color: rgba(0, 0, 0, 0.4); /* Transparent dark overlay */
            animation: fadeIn 0.3s ease-in-out;
            }


        /* Modal Content */
        .modal-content {
            background-color: #fefefe;
            margin: 5% auto;
            padding: 25px 30px;
            border: 5px solid #04AA6D; /* Thick border */
            border-radius: 16px;
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.3); /* Shadow effect */
            width: 90%;
            max-width: 500px;
            animation: scaleIn 0.35s ease-out;
            transition: box-shadow 0.3s ease;
            }

        .modal-content:hover {
            box-shadow: 0 0 25px rgba(4, 170, 109, 0.6); /* Glow on hover */
        }




        /* Close Button */
        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }


    </style>
</head>
<body>

<!-- The input button, placed at the bottom of the page -->
<div class="button-container">
    <button class="button button1" id="inputButton">Input</button>
</div>

<!-- The Modal -->
<div id="myModal" class="modal">
    <div class="modal-content">
        <span class="close" id="closeModal">&times;</span>
        <!-- Here, the form will be placed -->
        <h2>Enter Polynomial Data</h2>
        <form action="inputpoly.php" method="POST">
            First Factor: <input type="text" name="first_poly" required><br><br>
            Second Factor: <input type="text" name="second_poly" required><br><br>
            Third Factor: <input type="text" name="third_poly" required><br><br>
            <button type="submit">Submit</button>
        </form>
    </div>
</div>
<body>

    <!-- Banner Image -->
<div style="text-align: center; margin-top: 2px; margin-bottom: 2px;">
    <img src="banner.png" alt="Website Banner" style="width: 100%; height: auto;">
</div>

    

<script>
    // Get the modal
    var modal = document.getElementById("myModal");

    // Get the button that opens the modal
    var btn = document.getElementById("inputButton");

    // Get the <span> element that closes the modal
    var span = document.getElementById("closeModal");

    // When the user clicks the button, open the modal
    btn.onclick = function() {
        modal.style.display = "block";
    }

    // When the user clicks on <span> (x), close the modal
    span.onclick = function() {
        modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
        if (event.target == modal) {
            modal.style.display = "none";
        }
    }
</script>

</body>
</html>
